import Vue from 'vue';
import App from './App.vue';

// const AV = require('leancloud-storage');

// const { Query, User } = AV;

// Vue.config.productionTip = false

new Vue({
	render: h => h(App)
}).$mount('#app');
